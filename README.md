# kcl-ai

## install direnv

```shell
sudo apt install direnv
```
```shell
vim ~/.bashrc; source ~/.bashrc
# add `eval "$(direnv hook bash)"`
```
```shell
vim .envrc
# add KEY
```
```shell
direnv allow
```
